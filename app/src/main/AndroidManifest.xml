<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- 文件传输权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    
    <!-- 屏幕捕获权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <!-- 添加系统级权限以支持屏幕捕获 -->
    <uses-permission android:name="android.permission.CAPTURE_SECURE_VIDEO_OUTPUT" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" tools:ignore="ProtectedPermissions" />
    
    <!-- 辅助功能权限 -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    
    <!-- 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- 开机启动权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name="com.example.windowsandroidconnect.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        
        <activity

            android:name="com.example.windowsandroidconnect.MainActivity"

            android:exported="true"

            android:label="@string/app_name"

            android:theme="@style/Theme.MyApplication.NoActionBar">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <activity

            android:name="com.example.windowsandroidconnect.DeviceDiscoveryTestActivity"

            android:exported="true"

            android:label="设备发现测试"

            android:theme="@style/Theme.MyApplication.NoActionBar">

        </activity>

        

        <activity

            android:name="com.example.windowsandroidconnect.DebugTestActivity"

            android:exported="true"

            android:label="调试测试页面"

            android:theme="@style/Theme.MyApplication.NoActionBar">

        </activity>

        <activity
            android:name="com.example.windowsandroidconnect.SimpleConnectionTestActivity"
            android:exported="true"
            android:label="连接测试工具"
            android:theme="@style/Theme.MyApplication.NoActionBar">
        </activity>

        <activity
            android:name="com.example.windowsandroidconnect.QuickTestActivity"
            android:exported="false"
            android:label="快速功能验证测试" />

        <activity
            android:name="com.example.windowsandroidconnect.MockTestActivity"
            android:exported="false"
            android:label="模拟测试页面" />

        <!-- 屏幕捕获权限请求Activity -->
        <activity
            android:name="com.example.windowsandroidconnect.ScreenCaptureRequestActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- WebSocket连接服务 -->
        <service
            android:name="com.example.windowsandroidconnect.service.WebSocketConnectionService"
            android:enabled="true"
            android:exported="false" />

        

        <!-- 屏幕捕获服务 -->
        <service
            android:name="com.example.windowsandroidconnect.service.ScreenCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- 文件传输服务 -->
        <service
            android:name="com.example.windowsandroidconnect.service.FileTransferService"
            android:enabled="true"
            android:exported="false" />

        <!-- 设备发现服务 -->
        <service
            android:name="com.example.windowsandroidconnect.service.DeviceDiscoveryService"
            android:enabled="true"
            android:exported="false" />
            
        <!-- 通知服务 -->
        <service
            android:name="com.example.windowsandroidconnect.service.NotificationService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
            
        <!-- 剪贴板同步服务 -->
        <service
            android:name="com.example.windowsandroidconnect.service.ClipboardService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".ClientConfigActivity"
            android:label="客户端配置管理"
            android:exported="false" />

        <activity
            android:name="com.example.windowsandroidconnect.FileTransferActivity"
            android:exported="false"
            android:label="文件传输" />

        <service
            android:name="com.example.windowsandroidconnect.service.RemoteControlService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 屏幕捕获服务 -->
        <service
            android:name="com.example.windowsandroidconnect.ScreenCaptureService"
            android:enabled="true"
            android:exported="false" />

        <!-- 接收器 -->
        <receiver
            android:name="com.example.windowsandroidconnect.receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>